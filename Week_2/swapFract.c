#include <stdio.h>

struct fract_t {
        int num;
        int denum;
};

void swap(struct fract_t *a, struct fract_t *b){
    void swap(struct fract_t *a, struct fract_t *b)
}

int main(){
    return 0;
}